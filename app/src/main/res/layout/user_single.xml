<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/user_image"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:layout_marginBottom="10dp"
        android:layout_marginLeft="15dp"
        android:layout_marginTop="10dp"
        android:src="@drawable/if_user_925901" />

    <TextView
        android:id="@+id/user_nam"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/user_image"
        android:layout_marginStart="31dp"
        android:layout_marginTop="7dp"
        android:layout_toEndOf="@+id/user_image"
        android:text="TextView"
        android:textColor="@android:color/background_dark"
        android:textSize="18sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/user_stat"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignStart="@+id/user_nam"
        android:layout_below="@+id/user_nam"
        android:layout_marginTop="8dp"
        android:text="TextView"
        android:textSize="16sp" />

    <ImageButton
        android:id="@+id/locate"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:layout_marginEnd="15dp"
        android:background="@android:color/transparent"
        android:visibility="gone"
        app:srcCompat="@mipmap/ic_map_black_24dp" />

    <TextView
        android:id="@+id/status"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/user_nam"
        android:layout_marginStart="13dp"
        android:layout_toEndOf="@+id/user_nam"
        android:text="Running"
        android:textColor="@android:color/holo_green_dark"
        android:visibility="gone" />

</RelativeLayout>